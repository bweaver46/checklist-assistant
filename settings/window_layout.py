"""
Where the main window and the Playwright-controlled browser window open
on screen, so they land side by side instead of stacking on top of each
other. These are just numbers - if they land somewhere awkward on your
particular monitor, change them and re-run.

Coordinates are (x, y) from the top-left corner of your screen, in pixels.
"""

MAIN_WINDOW_POSITION = (50, 50)

BROWSER_WINDOW_POSITION = (550, 50)
BROWSER_WINDOW_SIZE = (1200, 850)
